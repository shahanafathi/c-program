#include <stdio.h>

int main()
{
    int num,i=1;
    printf("Enter the value : ");
    scanf("%d",&num);
    for(i=1;i<=10;i++)
    {
        printf( "%d * %d = %d \n",i,num,i*num);
    }
    return 0;
}
