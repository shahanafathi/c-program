#include <stdio.h>

int main()
{
  int a;
    printf("enter the value");
    scanf("%d",&a);
    if(a%2==0);
    {
        printf("its a even number")
    }
    else 
    {
        print ("its odd")
    }

    return 0;
}

