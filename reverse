#include <stdio.h>

int main()
{

  int num,remainder,rev;

  printf("Enter a value :  ");
  scanf("%d", &num);
  while (num>0) 
  {
    remainder=num%10;
    rev =rev*10+remainder;
    num=num/10;
  }
  printf("Reverse number = %d", rev);
  return 0;
}

